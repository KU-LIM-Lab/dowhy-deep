# gpu setting 
A100 GPU(NVIDIA A100 80GB PCle(4ea)) os(ubuntu 22.01), gpu driver(550.127.08), cuda(12.4)

# to-do
1. 도커 빌드
- 인터넷 끄고 테스트
- causal-analysis 컨테이너 이미지
    -> docker image 2개와 python, ollama, requirements 로컬파일 가져가기

2. 전처리
- 일단 앞에서 5000개 잘라서 동작 확인
- 희망직종 소분류별로 데이터 분리
(300만 -> 3만)
- 위 과정 병렬 처리

3. 학습
- tabpfn fit 으로 데이터 샘플링 -> 실험 전 1:99로 train/test split 진행
(3만 -> 1만)
- 성능좋았던 모델 fit 된 상태로 저장 
(pkl? 앞에 실험 성능 넘으면 덮어쓰기?) -> pkl파일은 각 실험별로 모두 저장될 예정

4. Treatment 
- 다중 트리트먼트 (n개 -> 2^n개)
(범주형, 연속형 트리트먼트 가능?)